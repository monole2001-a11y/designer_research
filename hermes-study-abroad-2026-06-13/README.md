# Hermes Study Abroad Notes

GitHub Pages-ready static site.

## Publish

1. Create or choose a GitHub repo.
2. Push this folder contents to the repo root, or to the `gh-pages` branch.
3. In GitHub: Settings → Pages → Deploy from branch → select branch/root.

Privacy: raw `ref/`, auth/config/session/runtime files are intentionally excluded.
